{
  "name": "prisma-101",
  "version": "1.0.0",
  "description": "This is a PrismaORM 101 ulitmate guide",
  "main": "server.ts",
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only server.ts",
    "db:migrate": "npx prisma migrate dev --name user-entity --create-only && npx prisma generate",
    "db:push": "npx prisma db push"
  },
  "author": "ostafinskim",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.8.6",
    "prisma": "^5.21.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@prisma/client": "^5.21.1"
  }
}
